اصلاح خطای build برای commit 08c2dfb

تغییرات:
- افزودن src/types/northwind.ts با انواع استخراج‌شده از ساختار واقعی JSON.
- حذف src/lib/gemini.ts: فایل قدیمی بدون مصرف‌کننده، دارای importهای ناموجود.
- ارتقای Next.js از 14.2.4 به 14.2.35 و افزودن package-lock.json.
- افزودن .gitignore و next-env.d.ts.

روش اعمال در GitHub:
1. فایل src/lib/gemini.ts را از ریپازیتوری حذف و commit کنید. آپلود فایل‌های جدید فایل قبلی را حذف نمی‌کند.
2. این ZIP را از حالت فشرده خارج کنید.
3. محتویات داخل پوشه BI-Assistant را در ریشه ریپازیتوری جایگزین کنید؛ ZIP را آپلود نکنید و پوشه اضافی نسازید.
4. تغییرات را commit کنید.
5. در Vercel استقرار commit جدید را بررسی کنید؛ commit قدیمی 08c2dfb را دوباره deploy نکنید.

اعتبارسنجی انجام‌شده:
- npm run build با Next.js 14.2.35 موفق شد، با بررسی TypeScript فعال.
- صفحه اصلی و API تحلیل پاسخ HTTP 200 دادند.
- جزئیات سفارش 10248 پاسخ معتبر داشت.
- چت بدون API key پیام نیاز به تنظیم کلید داد؛ ارتباط زنده Gemini آزمایش نشده است.

محدودیت مهم:
این اصلاح خطای ساخت است، نه تکمیل محصول BI. JSON موجود فقط 23 سفارش و 33 ردیف سفارش دارد و با دیتابیس کامل Northwind یکسان نیست. داده‌ها و منطق محاسبات در این اصلاح تغییر نکرده‌اند.
تغییرات روی GitHub شما push نشده‌اند و Vercel مستقیماً آزمایش نشده است.
